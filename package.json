{
  "name": "sequelize",
  "version": "1.0.0",
  "description": "Sequelize server to allow access to an SQL books database",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "M J MCCARTHY",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "sequelize": "^6.37.3"
  }
}
